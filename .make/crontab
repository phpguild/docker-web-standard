.PHONY: crontab/install

crontab/install:
ifeq (sudo, $(SUDO))
ifeq (prod, $(APP_ENV))
ifeq ("$(wildcard /etc/cron.d/$(APP_NAME)-$(APP_INSTANCE))","")
	$(SUDO) ln -s "$(shell pwd)/config/crontab/$(APP_INSTANCE)" "/etc/cron.d/$(APP_NAME)-$(APP_INSTANCE)"
endif
endif
endif